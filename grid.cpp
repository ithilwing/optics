/* #include "grid.h"

Grid::Grid(){}
LineOfSources Grid::ModifyLine (LineOfSources prevLine){
    LineOfSources newLine;
    for(int i = 0; i < numberOfSources; i ++){\
        if(prevLine.sources[i].x )
        newLine.sources.push_back();
    }
} */
